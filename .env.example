DJANGO_DEBUG=true
DJANGO_SECRET_KEY=change-me-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Leave DB_HOST empty to use SQLite for a quick local run.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=autobiz
DB_USER=autobiz
DB_PASSWORD=autobiz-local-only

# Add only when real AI integration is being tested.
OPENAI_API_KEY=
